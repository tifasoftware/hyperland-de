#!/bin/bash

find ~/.local/share/dotfiles/themes -mindepth 1 -maxdepth 1 \( -type d -o -type l \) | sort | while read -r path; do
  theme_name="$(basename "$path")"
  [[ "$theme_name" == "backgrounds" ]] && continue
  echo "$(echo "$theme_name" | sed -E 's/(^|-)([a-z])/\1\u\2/g; s/-/ /g')"
done
